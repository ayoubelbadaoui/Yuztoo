<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for push notifications on Android 13+ (API 33+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- NFC: required for reading and writing NFC tags. -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <!-- Camera: profile photo / merchant logo / merchant banner pickers in
         onboarding + account preferences (image_picker camera source) AND
         QR scanning (mobile_scanner). Declared here explicitly so the
         permission isn't only inherited from a transitive plugin manifest
         merge — this also lets us advertise the camera as an optional
         hardware feature so devices without one (rare on phones, common
         on tablets / TV) can still install the app. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <!-- BLE: proximity-based passage validation (Android 12+). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Legacy BLE permissions for Android < 12 (API 30 and below). -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <application
        android:label="YuzToo"
        android:name=".App"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Use app_links for vitrine URLs (see lib/core/config/vitrine_qr_config.dart). -->
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- https://yuztoo.app/vitrine/{merchantId} — opens client vitrine in-app when verified (optional: assetlinks.json). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="yuztoo.app" android:pathPrefix="/vitrine" />
                <data android:scheme="http" android:host="yuztoo.app" android:pathPrefix="/vitrine" />
            </intent-filter>
            <!-- yuztoo://vitrine/{merchantId} -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="yuztoo" android:host="vitrine" />
            </intent-filter>
            <!-- NFC tag tap → open the app directly and register the passage,
                 with no browser / no "ouvrir avec / rediriger ?" prompt.
                 NDEF_DISCOVERED is matched by Android's NFC tag-dispatch system
                 BEFORE the generic URL VIEW dispatch, so claiming the vitrine
                 URI here makes Yuztoo the handler even when the https App Link
                 is not (or cannot be) verified. MainActivity reads the NDEF URI
                 and hands the merchantId to Flutter (see MainActivity.kt). -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="https" android:host="yuztoo.app" android:pathPrefix="/vitrine" />
                <data android:scheme="http" android:host="yuztoo.app" android:pathPrefix="/vitrine" />
                <data android:scheme="yuztoo" android:host="vitrine" />
            </intent-filter>
        </activity>
        <!-- Required by image_cropper (uCrop library). -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- FCM default channel — IMPORTANCE_HIGH so banners appear on Samsung/Xiaomi/etc.
             Must match the channelId in Cloud Function and createNotificationChannel() in MainActivity. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="yuztoo_promo_v2" />
        <!-- FCM default notification icon (Android status bar).
             Points at the monochrome silhouette generated by
             `tools/build_notification_icon.py` from assets/branding/app_icon.png.
             Android strips RGB from this drawable and renders it tinted with
             the channel/system color — the bird-in-pin shape comes through
             from the alpha channel. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_yuztoo" />
        <!-- Tint the silhouette navy so the bird-in-pin matches the
             YuzToo app header colour on the lockscreen / status bar
             instead of the default device accent. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/yuztoo_navy" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
